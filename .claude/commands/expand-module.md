# Expand Module

1. Read the global `CLAUDE.md`.
2. Read the target module doc under `docs/systems/`.
3. Read the module-local `CLAUDE.md`.
4. Read the current task in `tasks/in-progress/`.
5. Keep changes inside the owning module and its direct dependencies.
6. Implement the smallest useful expansion first.
7. Document lasting decisions in `.claude/memory/decisions.md`.
